pipeline {
    agent any
    stages {
        stage('Pull Code') {
            steps {
                echo "Pulling code from GitHub..."
                git branch: 'main', url: 'https://github.com/username/bakery.git'
                echo "Code pulled successfully!"
            }
        }
    }
}
